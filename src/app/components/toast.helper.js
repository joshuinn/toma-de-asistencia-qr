import { toast } from "sonner";

//TODO icons, colors

export const toastSucces = (text) => {
    toast.success(text)
};

export const toastError = (text) => {
    /*toast.error(text,{
        icon:
    })*/
};
